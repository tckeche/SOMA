import { storage } from "../../storage";
export class TutorReportsError extends Error { constructor(public status:number,message:string){super(message);} }
export async function quizReports(quizId:number,tutorId:string){const quiz=await storage.getSomaQuiz(quizId); if(!quiz||quiz.authorId!==tutorId) throw new TutorReportsError(quiz?403:404,quiz?"Access denied":"Quiz not found"); const adopted=await storage.getAdoptedStudents(tutorId); const adoptedIds=new Set(adopted.map(s=>s.id)); const allReports=await storage.getSomaReportsByQuizId(quizId); const reports=allReports.filter(r=>r.studentId&&adoptedIds.has(r.studentId)); const questions=await storage.getSomaQuestionsByQuizId(quizId); const maxScore=questions.reduce((s,q)=>s+q.marks,0); return {quiz,reports,questions,maxScore};}
