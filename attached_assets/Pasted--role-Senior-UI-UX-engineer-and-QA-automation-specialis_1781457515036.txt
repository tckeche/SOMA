{
  "role": "Senior UI/UX engineer and QA automation specialist. You write production-grade Playwright suites, think in user journeys and failure modes, and back every claim with an artifact (screenshot, trace, log line, or failing assertion). No hand-waving.",
  "objective": "Run an exhaustive Playwright audit of Soma, apply only low-risk deterministic fixes, then produce a single Claude-ingestible report for a downstream overhaul.",
  "context": {
    "app": "Soma — AI-powered educational assessment platform (quizzes, exam-replica mode, per-student misconception tracking, LaTeX/symbolic answer entry, review links, multi-user roles, Google/Microsoft SSO).",
    "playwright": "Already installed and configured. Do not reinstall.",
    "credentials": "Use seeded test users for each role (student, tutor/admin). Never use real student data.",
    "priority_targets": [
      "Answer validation — LaTeX/symbolic/string-matching fallback. Hunt false negatives (correct answer marked wrong) from whitespace, \\dfrac vs /, decimal vs fraction equivalence, unicode vs proper sub/superscripts, sign placement, algebraic-equivalent forms.",
      "Review links — broken/expired/404, or loading the wrong question state."
    ],
    "branch": "qa/playwright-audit",
    "hard_constraints": [
      "Work only on the qa/playwright-audit branch.",
      "Never delete data or run destructive migrations.",
      "Never modify auth secrets or SSO config.",
      "If route list is large, run discovery and audit in batches by route group to avoid timeouts."
    ]
  },
  "execution_order": [
    "1. Build authenticated storage state per role so auth-gated pages are reachable.",
    "2. Map every route for unauthenticated and each authenticated role.",
    "3. Run the full audit across Chromium/Firefox/WebKit at viewports 375x812, 768x1024, 1440x900; trace=on, screenshot=on, video=retain-on-failure; artifacts to ./audit-artifacts/.",
    "4. Apply safe auto-fixes per policy, each with a proving test; re-run to green.",
    "5. Generate SOMA_AUDIT_REPORT.md.",
    "6. Print branch, commit list, report path, and inline executive summary."
  ],
  "audit_dimensions": {
    "functional": "Interact with every button, link, tab, dropdown, modal, toggle, form. Assert each produces the expected state change; flag any no-op or silent failure. Drive core journeys end-to-end: SSO sign-in (both providers), take quiz, exam-replica mode, submit answers, open review link, role-switching, tutor notification / auto-quiz flow.",
    "answer_validation": "PRIORITY. Build an input matrix of mathematically-equivalent answers and edge cases for symbolic/LaTeX questions. Record every false negative and false positive with the exact input.",
    "forms": "Submit valid, invalid, empty, boundary, and malformed inputs. Verify error messages, disabled/enabled states, and that no submission silently swallows errors.",
    "console": "Capture ALL console errors/warnings and uncaught exceptions per page. Zero-tolerance list.",
    "network": "Log all requests; flag 4xx/5xx, failed/aborted requests, broken API endpoints, and calls slower than 1s. Verify review-link endpoints resolve.",
    "links_nav": "Detect dead routes, 404s, broken in-app links, and review links loading wrong state.",
    "accessibility": "Run axe-core (WCAG 2.1 AA) on every page/state. Report contrast failures, missing alt/labels/ARIA, focus-order and keyboard-trap issues, and tab-reachability of all interactive elements.",
    "responsive": "Across all 3 viewports flag overflow, clipped/overlapping content, off-screen elements, broken layouts, and tap targets under 44x44px.",
    "performance": "Capture load time, LCP, CLS, and obvious asset/bundle bloat per route.",
    "states": "Verify empty states, loading states, error boundaries, and behaviour under throttled/offline network."
  },
  "auto_fix_policy": {
    "allowed": [
      "Broken/incorrect internal links and routing typos.",
      "Missing alt text, labels, trivially-addable ARIA.",
      "Console errors from undefined/null guards, missing keys, simple coercion.",
      "Unambiguous answer-validation normalisation (trim/whitespace/case/unicode-subscript) — only with a new proving test.",
      "Contrast tweaks within existing design tokens, tap-target sizing, clear responsive overflow fixes."
    ],
    "never": [
      "Architectural, data-model, auth/SSO, or scoring-logic changes that alter behaviour non-trivially.",
      "Anything you are less than 90 percent confident about.",
      "Anything touching more than a couple of files or changing a public contract."
    ],
    "rules": "Commit each fix atomically with its issue ID. Add/update a Playwright test that proves it. Re-run to confirm green. Everything not auto-fixed is flagged for Claude in the report."
  },
  "report": {
    "filename": "SOMA_AUDIT_REPORT.md",
    "requirement": "Self-contained enough for Claude to do a major overhaul from it alone. Specific over prose. Stable atomic IDs (SOMA-001...).",
    "sections": {
      "executive_summary": "Health score, counts by severity, top 5 themes.",
      "environment": "Commit SHA, browsers/viewports, what was and wasn't reachable.",
      "auto_fixed": "Table: ID | category | file(s) | change | proving test.",
      "open_issues": "One atomic entry each with: ID; Severity (Critical/High/Medium/Low); Category; Page/Route; File:line (best guess if uncertain); numbered deterministic Reproduction steps; Observed vs Expected; Root-cause hypothesis; concrete Proposed fix; Artifact path; Needs-Claude (yes/no); Risk (why not auto-fixed).",
      "prioritised_backlog": "Ordered first->last with rationale.",
      "coverage_gaps": "Journeys/states that couldn't be reached or tested, and why."
    }
  },
  "final_output": "Print path to SOMA_AUDIT_REPORT.md plus a one-paragraph TL;DR. Then stop."
}